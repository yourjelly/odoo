<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.ProductCard" owl="1">
        <button class="o_kiosk_product_card product-item btn btn-light d-flex flex-column p-2 pb-3 m-0 overflow-hidden"
            t-on-click="selectProduct"
            t-ref="selfProductCard">
            <div class="ratio ratio-1x1 mb-3">
                <img
                    class="o_self_order_item_card_image w-100 w-100 rounded bg-view"
                    t-attf-src="/menu/get-image/{{ props.product.id }}/512?unique={{props.product.write_date}}"
                    alt="Product image"
                    loading="lazy"
                    onerror="this.remove()" />
            </div>
            <div class="product-infos d-flex flex-column justify-content-between text-start flex-grow-1 w-100">
                <span t-esc="props.product.name" class="fs-3 fw-bolder mb-2" />
                <span t-esc="selfOrder.formatMonetary(props.product.prices)" class="fs-4 text-end text-muted" />
            </div>
        </button>
    </t>
</templates>

<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.LandingPage" owl="1">
        <KioskTemplate hideFooterBorder="true">
            <t t-set-slot="content">
                <div t-if="languages.length > 1" t-on-click="openLanguages" class="o_kiosk_language_selector position-absolute top-0 end-0 m-4 rounded p-4 bg-white shadow-lg">
                    <img class="rounded-1" t-attf-src="{{currentLanguage.flag_image_url}}" />
                    <span t-esc="currentLanguage.display_name" class="ms-3"></span>
                </div>
                <div t-if="selfOrder.kiosk_image_home.length > 0" t-on-click="start" class="o_kiosk_home_page d-flex flex-column vh-100 align-items-center overflow-hidden">
                    <div id="carouselAutoplaying" t-ref="carousel" class="carousel slide h-100 w-100" data-bs-ride="true">
                        <div class="carousel-inner h-100 w-100">
                            <div t-foreach="selfOrder.kiosk_image_home" t-as="image" t-key="image.id" t-attf-class="carousel-item h-100 w-100 {{activeImage}}">
                                <img t-attf-src="data:image/png;base64,{{image.data}}" class="o_kiosk-landing_image h-100 w-100"/>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="position-absolute end-0 bottom-0 start-0 text-center pb-4">
                    <button class="btn btn-lg btn-primary d-block mx-auto shadow-lg" t-on-click="start">TOUCH TO START</button>
                </div>
            </t>
        </KioskTemplate>
    </t>
</templates>

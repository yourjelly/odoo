<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_sale.ProductCard" t-inherit="point_of_sale.ProductCard" t-inherit-mode="extension">
        <xpath expr="//article" position="attributes">
            <attribute name="t-attf-class" separator=" " add=" {{ props.isDisabledCard ? 'disabled' : '' }}" />
        </xpath>
    </t>
</templates>

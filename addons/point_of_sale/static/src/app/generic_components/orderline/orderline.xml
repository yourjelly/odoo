<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="point_of_sale.Orderline">
        <t t-set="values" t-value="props.line"/>
        <t t-set="line" t-value="values.record"/>
        <li class="orderline p-2 lh-sm cursor-pointer"
            t-attf-class="{{ line.comboParent ? 'border-start border-3 ms-4' : '' }}"
            t-att-class="{ ...values.displayClasses, 'selected' : line.selected }">
            <div class="d-flex justify-content-between">
                <div class="product-name d-inline-block flex-grow-1 fw-bolder pe-1 text-truncate">
                    <span t-esc="values.fullProductName" class="text-wrap"/>
                    <t t-slot="product-name"/>
                </div>
                <div t-esc="env.utils.formatCurrency(values.displayPriceTotal)" class="price fw-bolder"/>
            </div>
            <ul class="info-list ms-2">
                <li>
                    <em t-esc="values.quantityStr" class="qty fst-normal fw-bolder me-1"/> <t t-if="values.unit" t-esc="values.unit.name"/>
                    x <s t-esc="env.utils.formatCurrency(values.unitPriceWoDiscount)" t-if="values.unitPriceWoDiscount"/>
                    <t t-esc="env.utils.formatCurrency(values.displayPriceUnit)"/> <span t-if="values.unit"> / <t t-esc="values.unit.name"/></span>
                </li>
                <li t-if="values.discount">
                    With a <em><t t-esc="values.discountStr"/>% </em> discount
                </li>
                <t t-slot="default"/>
                <li t-if="values.customerNote"
                    class="customer-note w-100 p-2 my-1 rounded text-break text-bg-warning text-warning bg-opacity-25">
                    <i class="fa fa-sticky-note me-1" role="img" aria-label="Customer Note" title="Customer Note"/>
                    <t t-esc="values.customerNote"/>
                </li>
                <li t-if="values.internalNote"
                    class="internal-note w-100 p-2 my-1 rounded text-bg-info text-info bg-opacity-25 text-break">
                    <i class="fa fa-tag me-1" role="img" aria-label="Note" title="Note"/>
                    <t t-esc="values.internalNote"/>
                </li>
            </ul>
        </li>
    </t>
</templates>

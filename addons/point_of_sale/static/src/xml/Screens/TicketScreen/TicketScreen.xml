<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="TicketScreen" owl="1">
        <div class="ticket-screen screen" t-att-class="{ oe_hidden: !props.isShown }">
            <div class="screen-full-width">
                <div class="rightpane pane-border">
                    <div class="controls">
                        <t t-if="!env.isMobile || !state.showSearchBar">
                            <div class="buttons">
                                <button class="discard" t-on-click="onCloseScreen">
                                    <span class="search-icon">
                                        <i class="fa fa-angle-double-left"/>
                                    </span>
                                    <t t-if="!env.isMobile">
                                        Back
                                    </t>
                                </button>
                                <button t-if="shouldShowNewOrderButton()" class="highlight" t-on-click="_onCreateNewOrder">New Order</button>
                            </div>
                        </t>
                        <t t-if="env.isMobile">
                            <t t-if="state.showSearchBar">
                                <button class="arrow-left" t-on-click="() => { state.showSearchBar = !state.showSearchBar }">
                                    <span class="search-icon">
                                        <i class="fa fa-angle-double-left"/>
                                    </span>
                                </button>
                            </t>
                            <t t-else="">
                                <button class="search" t-on-click="() => { state.showSearchBar = !state.showSearchBar }">
                                    <span class="search-icon">
                                        <i class="fa fa-search"/>
                                    </span>
                                </button>
                            </t>
                        </t>
                        <t t-set="_placeholder">Search Orders...</t>
                        <SearchBar t-if="state.showSearchBar" config="getSearchBarConfig()" placeholder="_placeholder"
                                   onFilterSelect.bind="onFilterSelect" onSearch.bind="onSearch"
                        />
                        <div t-if="shouldShowPageControls()" class="item">
                            <div class="page-controls">
                                <div class="previous" t-on-click="_onPrevPage">
                                    <i class="fa fa-fw fa-caret-left" role="img" aria-label="Previous Order List" title="Previous Order List"></i>
                                </div>
                                <div class="next" t-on-click="_onNextPage">
                                    <i class="fa fa-fw fa-caret-right" role="img" aria-label="Next Order List" title="Next Order List"></i>
                                </div>
                            </div>
                            <div class="page">
                                <span><t t-esc="getPageNumber()" /></span>
                            </div>
                        </div>
                    </div>
                    <div class="orders">
                        <OrderList orders="_getFormattedOrders()" onClickOrderDetails.bind="onClickOrderDetails"
                                   selectedOrderUid="state.selectedOrder ? state.selectedOrder.uid : null" showCardholderName="_showCardholderName()"
                                   onDeleteOrder.bind="onDeleteOrder" closeTicketScreen.bind="onCloseScreen"
                        />
                    </div>
                </div>
                <div class="leftpane">
                    <t t-set="_selectedSyncedOrder" t-value="getSelectedSyncedOrder()" />
                    <OrderDetails order="_selectedSyncedOrder" selectedOrderlineId="state.selectedOrderlineId" highlightHeaderNote="_state.ui.highlightHeaderNote" />
                    <div class="pads">
                        <div class="control-buttons">
                            <InvoiceButton order="_selectedSyncedOrder" onInvoiceOrder.bind="onInvoiceOrder" />
                            <ReprintReceiptButton order="_selectedSyncedOrder" />
                        </div>
                        <div class="subpads">
                            <!-- We set so that the term 'Refund' is translated. -->
                            <t t-set="_actionName">Refund</t>
                            <ActionpadWidget partner="getSelectedPartner()" actionName="_actionName" actionToTrigger="'do-refund'" isActionButtonHighlighted="getHasItemsToRefund()" />
                            <NumpadWidget disabledModes="['price', 'discount']" activeMode="state.selectedOrderlineId and 'quantity'" disableSign="true" />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>

</templates>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- <record id="view_pos_move_form" model="ir.ui.view">
        <field name="name">pos.move.form</field>
        <field name="model">pos.move</field>
        <form string="Other Transactions" create="0">
            <field name="arch" type="xml">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_invoice" string="Invoice" type="object" class="oe_stat_button"
                            icon="fa-pencil-square-o" attrs="{'invisible':[('state','!=','invoiced')]}">
                        </button>
                    </div>
                    <group col="4" colspan="4" name="move_fields">
                        <field name="name" />
                        <field name="date" />
                        <field name="session_id" />
                        <field string="User" name="user_id" />
                        <field name="partner_id" context="{'res_partner_search_mode': 'customer'}"
                            attrs="{'readonly': [('state','=','invoiced')]}" />
                        <field name="fiscal_position_id" options="{'no_create': True}" />
                        <field name="invoice_group" invisible="1" />
                    </group>
                    <notebook colspan="4">
                        <page string="Products" name="products">
                            <field name="lines" colspan="4" nolabel="1">
                                <tree string="Order lines" editable="bottom">
                                    <field name="full_product_name" />
                                    <field name="pack_lot_ids" widget="many2many_tags"
                                        groups="stock.group_production_lot" />
                                    <field name="qty" />
                                    <field name="product_uom_id" string="UoM" groups="uom.group_uom" />
                                    <field name="price_unit" widget="monetary" />
                                    <field name="discount" string="Disc.%" widget="monetary" />
                                    <field name="tax_ids_after_fiscal_position" widget="many2many_tags"
                                        string="Taxes" />
                                    <field name="tax_ids" widget="many2many_tags" invisible="1" />
                                    <field name="price_subtotal" widget="monetary" force_save="1" />
                                    <field name="price_subtotal_incl" widget="monetary" force_save="1" />
                                    <field name="currency_id" invisible="1" />
                                </tree>
                                <form string="Order lines">
                                    <group col="4">
                                        <field name="full_product_name" />
                                        <field name="qty" />
                                        <field name="discount" widget="monetary" />
                                        <field name="price_unit" widget="monetary" />
                                        <field name="price_subtotal" invisible="1" widget="monetary" force_save="1" />
                                        <field name="price_subtotal_incl" invisible="1" widget="monetary"
                                            force_save="1" />
                                        <field name="tax_ids_after_fiscal_position" widget="many2many_tags"
                                            string="Taxes" />
                                        <field name="tax_ids" widget="many2many_tags" invisible="1" />
                                        <field name="pack_lot_ids" widget="many2many_tags"
                                            groups="stock.group_production_lot" />
                                        <field name="notice" />
                                        <field name="currency_id" invisible="1" />
                                    </group>
                                </form>
                            </field>
                            <group class="oe_subtotal_footer oe_right" colspan="2" name="order_total">
                                <field name="amount_tax" force_save="1" widget="monetary" />
                                <div class="oe_subtotal_footer_separator oe_inline">
                                    <label for="amount_total" />
                                    <button name="button_dummy" states="draft" string="(update)"
                                        class="oe_edit_only oe_link" />
                                </div>
                                <field name="amount_total" force_save="1" nolabel="1"
                                    class="oe_subtotal_footer_separator" widget="monetary" />
                                <field name="amount_paid" string="Total Paid (with rounding)"
                                    class="oe_subtotal_footer_separator" widget="monetary"
                                    attrs="{'invisible': [('amount_paid','=', 'amount_total')]}" />
                                <field name="currency_id" invisible="1" />
                            </group>
                            <div class="oe_clear" />
                        </page>
                        <page string="Payments" name="payments">
                            <field name="payment_ids" colspan="4" nolabel="1">
                                <tree string="Payments">
                                    <field name="currency_id" invisible="1" />
                                    <field name="payment_date" />
                                    <field name="payment_method_id" />
                                    <field name="amount" />
                                </tree>
                            </field>
                            <group class="oe_subtotal_footer oe_right" colspan="2" name="order_total">
                                <field name="amount_tax" force_save="1" widget="monetary" />
                                <div class="oe_subtotal_footer_separator oe_inline">
                                    <label for="amount_total" />
                                    <button name="button_dummy" states="draft" string="(update)"
                                        class="oe_edit_only oe_link" />
                                </div>
                                <field name="amount_total" force_save="1" nolabel="1"
                                    class="oe_subtotal_footer_separator" widget="monetary" />
                                <field name="amount_paid" string="Total Paid (with rounding)"
                                    class="oe_subtotal_footer_separator" widget="monetary"
                                    attrs="{'invisible': [('amount_paid','=', 'amount_total')]}" />
                                <field name="currency_id" invisible="1" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </field>
        </form>
        </field>
    </record>

    <record model="ir.ui.view" id="view_pos_order_kanban">
        <field name="name">pos.order.kanban</field>
        <field name="model">pos.order</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" create="0" sample="1">
                <field name="name" />
                <field name="partner_id" />
                <field name="amount_total" />
                <field name="date_order" />
                <field name="state" />
                <field name="pos_reference" />
                <field name="partner_id" />
                <field name="currency_id" />
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <span t-if="record.partner_id.value">
                                            <t t-esc="record.partner_id.value" />
                                        </span>
                                        <span t-else="">
                                            <t t-esc="record.name.value" />
                                        </span>
                                    </strong>
                                </div>
                                <strong>
                                    <field name="amount_total" widget="monetary" />
                                </strong>
                            </div>
                            <div class="row">
                                <div class="col-12">
                                    <span>
                                        <t t-esc="record.pos_reference.value" />
                                    </span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-8 text-muted">
                                    <span>
                                        <t t-esc="record.date_order.value" />
                                    </span>
                                </div>
                                <div class="col-4">
                                    <span class="float-right text-right">
                                        <field name="state" widget="label_selection" options="{'classes': {'draft': 'default',
                                        'invoiced': 'success', 'cancel': 'danger'}}" />
                                    </span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record> -->

    <record id="action_pos_move" model="ir.actions.act_window">
        <field name="name">Other Transactions</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pos.move</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" eval="False" />
        <field name="domain">[]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No other transactions found
            </p>
            <p>
                To record transactions, start a new session.
            </p>
        </field>
    </record>

    <record id="view_pos_move_tree" model="ir.ui.view">
        <field name="name">pos.move.tree</field>
        <field name="model">pos.move</field>
        <field name="arch" type="xml">
            <tree string="Other Transactions" create="0" sample="1">
                <field name="name" decoration-bf="1" />
                <field name="session_id" />
                <field name="date" />
                <field name="partner_id" />
            </tree>
        </field>
    </record>

    <!-- <record id="view_pos_order_search" model="ir.ui.view">
        <field name="name">pos.order.search.view</field>
        <field name="model">pos.order</field>
        <field name="arch" type="xml">
            <search string="Point of Sale Orders">
                <field name="name" />
                <field name="config_id" />
            </search>
        </field>
    </record> -->

    <menuitem id="menu_pos_move" parent="menu_point_of_sale" action="action_pos_move" sequence="2"
        groups="group_pos_manager,group_pos_user" />

</odoo>
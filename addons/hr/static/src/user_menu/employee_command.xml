<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="hr.EmployeeCommand" owl="1">
        <div class="o_command_default d-flex align-items-center px-4 py-2">
            <img t-att-src="props.avatar_url" class="d-inline-block pe-2 rounded-1" style="max-width: 24px; max-height: 24px; height: auto; width: auto;"/>
            <span class="pe-2 text-ellipsis">
                <t t-slot="name" /> <span class="small" t-if="props.job">(<t t-esc="props.job"/>)</span>
            </span>
            <!-- <icon t-if="props.isFavorite" class="fa fa-star o_favorite pe-2 position-static" /> -->
            <span class="ms-auto flex-shrink-0">
                <t t-slot="focusMessage"/>
            </span>
        </div>
    </t>

    <t t-name="hr.EmployeeAdvancedSearchCommand" owl="1">
        <div class="o_command_hotkey d-flex align-items-center justify-content-between px-4 py-2 cursor-pointer">
        <span>
            <icon class="fa fa-users pe-2"/>
            <t t-slot="name"/>
        </span>
        <span>
            <t t-foreach="getKeysToPress(props)" t-as="key" t-key="key_index">
            <kbd t-out="key" class="d-inline-block px-3 py-1" />
            <span t-if="!key_last"> + </span>
            </t>
        </span>
        </div>
    </t>
</templates>

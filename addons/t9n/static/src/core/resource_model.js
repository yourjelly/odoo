export class Resource {
    constructor(id, fileName, projectId, messages) {
        this.id = id;
        this.fileName = fileName;
        this.projectId = projectId;
        this.messages = messages;
    }
}

<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form_razorpay" model="ir.ui.view">
        <field name="name">Razorpay Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position="before">
                <div
                     name="razorpay_onboarding_group"
                     invisible="context.get('razorpay_onboarding', False) or (code != 'razorpay' or razorpay_key_secret or razorpay_key_id)">
                    <button string="Connect Razorpay"
                            name="action_razorpay_connect_account"
                            type="object"
                            class="btn-primary"
                            colspan="2"
                            invisible="state == 'enabled'"/>
                </div>
            </group>
            <group name="provider_credentials" position='inside'>
                <group name="razorpay_credentials"
                       invisible="code != 'razorpay'">
                    <field name="razorpay_key_id"
                           string="Key Id"
                           required="code == 'razorpay' and state != 'disabled'"/>
                    <field name="razorpay_key_secret"
                           string="Key Secret"
                           required="code == 'razorpay' and state != 'disabled'"
                           password="True"/>
                            <!-- <field name="razorpay_webhook_secret"
                                string="Webhook Secret"
                                required="code == 'razorpay' and state != 'disabled'"
                                password="True"/> -->
                    <label for="razorpay_webhook_secret"/>
                    <div class="o_row" col="2">
                        <field name="razorpay_webhook_secret"
                               password="True"/>
                        <button string="Generate your webhook"
                                type="object"
                                name="action_razorpay_create_webhook"
                                class="btn-primary"
                                invisible="razorpay_webhook_secret or not razorpay_key_secret"/>
                    </div>
                </group>
            </group>
            <field name="allow_tokenization" position="after">
                <div invisible="code != 'razorpay' or not allow_tokenization" colspan="2">
                    <a href="https://www.odoo.com/documentation/17.0/applications/finance/payment_providers/razorpay.html#payment-providers-razorpay-recurring-payments"
                       class="btn btn-link"
                       role="button"
                       target="new"
                    >
                        <i class="oi oi-fw o_button_icon oi-arrow-right"/>
                        Enable recurring payments on Razorpay
                    </a>
                </div>
            </field>
        </field>
    </record>

    <record id="action_razorpay_payment_provider_onboarding" model="ir.actions.act_window">
        <field name="name">Payment Providers</field>
        <field name="res_model">payment.provider</field>
        <field name="view_mode">form</field>
        <field name="context">{'razorpay_onboarding': True}</field>
    </record>

</odoo>

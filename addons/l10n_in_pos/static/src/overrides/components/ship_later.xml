<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="l10n_in_pos.ShipLater" t-inherit="point_of_sale.DatePickerPopup" t-inherit-mode="primary">
        <xpath expr="//input" position="after">
            <!-- <input class="form-control form-control-lg w-75 mx-auto" type="text" t-model="props.inputValue" t-ref="input" placeholder="Enter Place Of Supply"/> -->
            <!-- <Many2OneField t-props="stateId"/> -->
            <!-- <Dropdown class="d-flex">
                <span>Enter the place of supply</span>
                <DropdownItem t-foreach="getPos" t-as="pos" t-key="pos">
                    <t t-out="pos"/>
                </DropdownItem>
            </Dropdown> -->

            <div class="o_input_dropdown">
                <AutoComplete
                    value="''"
                    placeholder="'Enter Place of supply'"
                    sources="sources"
                    onSelect.bind="onSelect"
                    dropdown="true"
                    autoSelect="true"
                    autofocus="true"
                />
            </div>
        </xpath>
    </t>
</templates>

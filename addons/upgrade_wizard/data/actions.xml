<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_run_checks" model="ir.actions.server">
      <field name="name">Run checks</field>
      <field name="type">ir.actions.server</field>
      <field name="model_id" ref="model_upgradewizard_check" />
      <field name="binding_model_id" ref="model_upgradewizard_check"/>
      <field name="state">code</field>
      <field name="code">
for rec in records:
    rec.run_check()
    </field>
    </record>

</odoo>

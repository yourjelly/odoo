<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_users_form" model="ir.ui.view">
            <field name="name">res.users.form.inherit</field>
            <field name="model">res.users</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='preferences']" position="after">
                    <page string="Oauth" name="oauth">
                        <group>
                            <field name="oauth_provider_id"/>
                            <field name="oauth_uid"/>
                            <field name="oauth_access_token"/>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>

        <record id="identity_check_wizard" model="ir.ui.view">
            <field name="model">res.users.identitycheck.oauth</field>
            <field name="inherit_id" ref="base.identity_check_wizard"/>
            <field name="arch" type="xml">
                <field name="oauth_uid" invisible="True"/>
                <xpath expr="//a[@href='/web/reset_password/']" position="replace">
                    <p attrs="{'invisible': [('oauth_uid', '=', False)]}">
                        Using an external authentication and don't have a password defined?<br/>
                        <a href="/web/reset_password/" role="button">Click here</a> to receive a reset password email.
                    </p>
                    <p attrs="{'invisible': [('oauth_uid', '!=', False)]}">
                        <a href="/web/reset_password/" class="btn btn-link" role="button">Forgot password?</a>
                    </p>
                </xpath>
                </form>
            </field>
        </record>

</odoo>

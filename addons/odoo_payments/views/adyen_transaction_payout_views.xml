<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="adyen_transaction_payout_list" model="ir.ui.view">
        <field name="name">adyen.transaction.payout.list</field>
        <field name="model">adyen.transaction.payout</field>
        <field name="priority">10</field>
        <field name="arch" type="xml">
            <tree js_class="adyen_transactions_list" expand="1">
                <field name="currency_id" invisible="1" />
                <field name="reference" optional="show" />
                <field name="bank_account_id" />
                <field name="amount" string="Amount" widget="monetary" />
                <field name="date"/>
                <field name="status"/>
                <field name="company_id" groups="base.group_multi_company" optional="show"/>
            </tree>
        </field>
    </record>

    <record id="adyen_transaction_payout_search" model="ir.ui.view">
        <field name="name">adyen.transaction.payout.search</field>
        <field name="model">adyen.transaction.payout</field>
        <field name="arch" type="xml">
            <search string="Payouts">
                <field name="reference" />
                <field name="status" />
                <filter name="filter_date" date="date" string="Transaction Date" />
                <group>
                    <filter string="Currency" name="groupby_currency_id" context="{'group_by': 'currency_id'}"/>
                    <filter string="Date" name="groupby_date" context="{'group_by': 'date'}" />
                    <filter string="Company" name="groupby_company_id"
                        groups="base.group_multi_company" context="{'group_by': 'company_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_view_adyen_transaction_payout" model="ir.actions.act_window">
        <field name="name">Balance</field>
        <field name="res_model">adyen.transaction.payout</field>
        <field name="view_mode">tree</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No payouts yet.
            </p>
        </field>
    </record>

</odoo>

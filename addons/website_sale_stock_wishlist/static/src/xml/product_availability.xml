<?xml version="1.0" encoding="UTF-8"?>

<templates>
    <t t-name="website_sale_stock_wishlist.product_availability" inherit_id="website_sale_stock.product_availability">
        <div id="stock_wishlist_message" t-if="product_type == 'product' and is_published and !free_qty and allow_to_order != 'always' and !wish" t-attf-class="availability_message_#{product_template} text-success mt8">
            Add the item to your wishlist to be notified when the product is back in stock.
        </div>
    </t>
</templates>

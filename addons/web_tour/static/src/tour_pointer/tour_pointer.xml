<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="web_tour.TourPointer" owl="1">
        <div
            t-if="props.pointerState.isVisible"
            t-ref="popper"
            t-attf-class="
                o_tour_pointer
                o_{{ position }}
                {{ isOpen ? 'o_open' : 'o_bouncing' }}
                {{ props.pointerState.fixed ? 'position-fixed' : 'position-absolute' }}
                {{ props.pointerState.onClick ? 'cursor-pointer' : '' }}
            "
            t-attf-style="
                --TourPointer__width: {{ constructor.width }}px;
                --TourPointer__height: {{ constructor.height }}px;
            "
            t-on-mouseenter="() => toggleOpen(true)"
            t-on-mouseleave="() => toggleOpen(false)"
            t-on-click="props.pointerState.onClick || (() => {})"
        >
            <div class="o_tour_pointer_tip position-absolute" />
            <div
                class="o_tour_pointer_content overflow-hidden px-3 py-2 w-100 h-100 position-relative"
                t-att-class="{ invisible: !isOpen }"
            >
                <t t-out="content" />
            </div>
        </div>
    </t>
</templates>

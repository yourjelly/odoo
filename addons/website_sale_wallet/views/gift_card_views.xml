<?xml version="1.0"?>
<odoo>
    <!-- Actions -->
    <record id="gift_card_sale_order_action" model="ir.actions.act_window">
        <field name="name">Gift Cards</field>
        <field name="res_model">gift.card</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('sale_order_id', '=', active_id)]</field>
    </record>
    <record id="gift_card_action" model="ir.actions.act_window">
        <field name="name">Gift Cards</field>
        <field name="res_model">gift.card</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_valid': True}</field>
    </record>
    <!-- view mode -->
    <record id="gift_card_view_tree" model="ir.ui.view">
        <field name="name">gift.card.tree</field>
        <field name="model">gift.card</field>
        <field name="arch" type="xml">
            <tree string="Gift Card">
                <field name="code"/>
                <field name="amount"/>
                <field name="state"/>
            </tree>
        </field>
    </record>
    <record id="gift_card_view_form" model="ir.ui.view">
        <field name="name">gift.card.form</field>
        <field name="model">gift.card</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button string="Add Gift Card to partner's wallet"
                            name="action_add_card" type="object"
                            attrs="{'invisible': [('state', '!=', 'new')]}"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="website_id" attrs="{'readonly': [('state', '!=', 'new')]}"/>
                            <field name="sale_order_id"/>
                            <field name="create_date"/>
                            <field name="expired_date" attrs="{'readonly': [('state', '!=', 'new')]}"/>
                            <field name="partner_id" attrs="{'readonly': [('state', '!=', 'new')]}"/>
                        </group>
                        <group>
                            <field name="currency_id"/>
                            <field name="code"/>
                            <field name="amount"/>
                            <field name="state"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <!-- Searching -->
    <record id="gift_card_view_search" model="ir.ui.view">
        <field name="name">gift.card.search</field>
        <field name="model">gift.card</field>
        <field name="arch" type="xml">
            <search string="Gift Card">
                <field name="code"/>
                <filter name="valid"
                        string="Available"
                        domain="[('state', 'in', ['new', 'used'])]"
                        help="Show only valid"/>
            </search>
        </field>
    </record>
</odoo>

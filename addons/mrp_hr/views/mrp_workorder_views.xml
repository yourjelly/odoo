<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="mrp_hr_production_workorder_tree_view_inherit" model="ir.ui.view">
        <field name="name">mrp.hr.production.work.order.tree.inherit</field>
        <field name="model">mrp.workorder</field>
        <field name="inherit_id" ref="mrp.mrp_production_workorder_tree_editable_view"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="multi_edit">1</attribute>
            </xpath>
            <field name="workcenter_id" position="after">
                <field name="employee_id"/>
            </field>
        </field>
    </record>

    <record id="mrp_hr_production_workorder_form_view_inherit" model="ir.ui.view">
        <field name="name">mrp.hr.production.work.order.form.inherit</field>
        <field name="model">mrp.workorder</field>
        <field name="inherit_id" ref="mrp.mrp_production_workorder_form_view_inherit"/>
        <field name="arch" type="xml">
            <field name="production_id" position="after">
                <field name="employee_id"/>
            </field>
        </field>
    </record>

    <record id="view_mrp_production_workorder_form_view_filter_inherit" model="ir.ui.view">
        <field name="name">mrp.production.work.order.select</field>
        <field name="model">mrp.workorder</field>
        <field name="inherit_id" ref="mrp.view_mrp_production_workorder_form_view_filter"/>
        <field name="arch" type="xml">
            <field name="production_id" position="after">
                <field name="employee_id"/>
            </field>
            <filter name="production" position="after">
                <filter string="Employee" name="employee" domain="[]" context="{'group_by': 'employee_id'}"/>
            </filter>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_mrp_workorder_employee">
        <field name="name">Work Orders Planning</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">mrp.workorder</field>
        <field name="domain">[('production_state','not in',('done','cancel'))]</field>
        <field name="view_mode">gantt,tree,form,calendar,pivot,graph</field>
        <field name="search_view_id" ref="view_mrp_production_workorder_form_view_filter_inherit"/>
        <field name="view_id" ref="mrp.workcenter_line_gantt_production"/>
        <field name="context">{'search_default_employee': True, 'search_default_ready': True, 'search_default_waiting': True, 'search_default_progress': True, 'search_default_pending': True}</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            No work orders to do!
          </p><p>
            Work orders are operations to do as part of a manufacturing order.
            Operations are defined in the bill of materials or added in the manufacturing order directly.
          </p>
        </field>
    </record>

</odoo>

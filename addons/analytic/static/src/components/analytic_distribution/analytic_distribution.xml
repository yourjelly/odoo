<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="analytic.AnalyticDistribution">
        <div class="o_field_tags d-inline-flex flex-wrap mw-100" t-att-class="{'o_tags_input o_input': !props.readonly}" t-ref="analyticDistribution" t-on-keydown="onWidgetKeydown">
            <TagsList tags="planSummaryTags()"/>
            <div t-if="!props.readonly" class="o_input_dropdown d-inline-flex w-100" tabindex="0" t-ref="mainElement" t-on-focus="onMainElementFocus" t-on-click="onMainElementFocus">
                <span class="analytic_distribution_placeholder"/>
                <span class="o_dropdown_button" />
                <t t-call="analytic.AnalyticDistributionPopup"/>
            </div>
        </div>
    </t>

    <t t-name="analytic.AnalyticDistributionPopup">
        <div class="analytic_distribution_popup dropdown-menu o-dropdown--menu show rounded py-0 overflow-x-hidden" t-if="state.showDropdown" t-ref="analyticDropdown">
            <div class="popover-header sticky-top">
                <div class="d-flex">
                    <div class="h5 mt-2 me-auto">Analytic</div>
                    <div class="popupButtons">
                        <span t-if="planSummaryTags() and allowSave" class="btn o_button" t-on-click="onSaveNew" title="Save as new analytic distribution model"><i class="fa fa-save"/></span>
                        <span class="btn o_button" t-on-click.stop="() => this.closeAnalyticEditor()" title="Close"><span class="fa fa-close"/></span>
                    </div>
                </div>
            </div>
            <div class="p-2 table-responsive">
                <t t-set="totals" t-value="planTotals()"/>
                <table class="o_list_table table table-sm table-hover o_analytic_table mb-2 table-striped">
                    <thead>
                        <tr class="border-bottom">
                            <th t-foreach="allPlans" t-as="plan" t-key="plan.id">
                                <span t-out="plan.name"/>
                                (<span t-att-class="totals[plan.id].class">
                                    <t t-out="totals[plan.id].formattedValue"/>
                                </span>)
                            </th>
                            <th t-out="'Percentage'" class="numeric_column_width"/>
                            <th t-if="valueColumnEnabled" class="numeric_column_width" t-out="props.record.fields[props.amount_field].string"/>
                            <th class="deleteColumn w-20px"/>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="state.formattedData" t-as="line" t-key="line.id" t-att-id="line_index" t-att-name="'line_' + line_index">
                            <Record t-props="recordProps(line)" t-slot-scope="data">
                                <!-- loop through data.record.fields instead? -->
                                <t t-foreach="Object.entries(data.record.data)" t-as="field" t-key="field[0]">
                                    <!-- Is there a better way to hide the currency field -->
                                    <td t-if="!data.record.fields[field[0]].invisible and (field[0] != props.amount_field or valueColumnEnabled)" t-att-class="data.record.fields[field[0]].cellClass">
                                        <!-- Field works well for the Many2one field -->
                                        <Field t-if="field[0].startsWith('x_plan')" id="field[0]" name="field[0]" record="data.record" domain="data.record.fields[field[0]].domain" canOpen="false" canCreate="false" canCreateEdit="false" canQuickCreate="false"/>
                                        <!-- <Field t-if="field[0].startsWith('x_plan')" id="field[0]" name="field[0]" record="data.record" domain="data.record.fields[field[0]].domain" canOpen="false" canCreate="false" canCreateEdit="false"/> -->
                                        <!-- PercentageField is recommended -->
                                        <Field t-if="field[0] == 'percentage'" id="'percentage'" name="'percentage'" record="data.record"/>
                                        <Field t-if="valueColumnEnabled and field[0] == props.amount_field" id="props.amount_field" name="props.amount_field" record="data.record"/>
                                        <!-- <Field t-if="props.amount_field and field[0] == data.record.fields[props.amount_field].currency_field" id="data.record.fields[props.amount_field].currency_field" name="data.record.fields[props.amount_field].currency_field" record="data.record"/> -->
                                    </td>
                                </t>
                                <td class="w-20px">
                                    <span class="fa fa-trash-o cursor-pointer" t-on-click.stop="() => this.deleteLine(line_index)"/>
                                </td>
                            </Record>
                        </tr>
                        <tr>
                            <td t-on-click.stop.prevent="addLine" class="o_field_x2many_list_row_add" t-att-colspan="allPlans.length + 2 + valueColumnEnabled">
                                <a href="#" t-ref="addLineButton" tabindex="0">Add a Line</a>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </t>

</templates>

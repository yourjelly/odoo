from odoo import api,models,fields


class fitterExpandableRegister(models.Model):
    _name = "fitter_expandable.register"
    _inherit = "expandable.register"
    _description = "Expandable Register (Fitter)"
    
    # Fields expandable registrer contains
class wiremanExpandableRegister(models.Model):
    _name = "wireman_expandable.register"
    _inherit = "expandable.register"
    _description = "Expandable Register (wireman)"
    
    # Fields expandable registrer contains

from odoo import api,models,fields

class mechanicDieselExpandableRegister(models.Model):
    _name = "mechanic_diesel_expandable.register"
    _inherit = "expandable.register"
    _description = "Expandable Register (Mechanic Diesel)"
    
    # Fields expandable registrer contains

from odoo import api,models,fields

class armatureMotorRewindingExpandableRegister(models.Model):
    _name = "armature_motor_rewinding_expandable.register"
    _inherit = "expandable.register"
    _description = "Expandable Register (Armature Motor Rewinding)"
    
    # Fields expandable registrer contains

from odoo import api,models,fields

class sewingTechnologyExpandableRegister(models.Model):
    _name = "sewing_technology_expandable.register"
    _inherit = "expandable.register"
    _description = "Expandable Register (Sewing Technology)"
    
    # Fields expandable registrer contains
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Views -->
    <record id="project_tags_view_search" model="ir.ui.view">
        <field name="name">Tags</field>
        <field name="model">project.tags</field>
        <field name="arch" type="xml">
            <search string="Issue Version">
                <field name="name"/>
            </search>
        </field>
    </record>

    <record id="project_tags_view_form" model="ir.ui.view">
        <field name="name">Tags</field>
        <field name="model">project.tags</field>
        <field name="arch" type="xml">
            <form string="Tags">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="project_tags_view_tree" model="ir.ui.view">
        <field name="name">Tags</field>
        <field name="model">project.tags</field>
        <field name="arch" type="xml">
            <tree string="Tags" editable="bottom" sample="1">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </tree>
        </field>
    </record>

    <!-- Actions -->
    <record id="project_tags_action" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">project.tags</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            No tags found. Let's create one!
            </p>
            <p>
                Tags are perfect to categorize your tasks.
            </p>
        </field>
    </record>
    
    <menuitem id="project_tags_menu"
        action="project_tags_action" 
        parent="project.project_project_menu_config"/>
</odoo>
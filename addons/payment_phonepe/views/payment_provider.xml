<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form_ccavenue" model="ir.ui.view">
        <field name="name">payment.provider.form.inherit</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position='inside'>
                <group name="phonepe_credentials" invisible="code != 'phonepe'">
                    <field name="phonepe_merchant_id" password="True" required="code == 'phonepe' and state != 'disabled'"/>
                    <field name="phonepe_salt_key" password="True" required="code == 'phonepe' and state != 'disabled'"/>
                    <field name="phonepe_salt_index" required="code == 'phonepe' and state != 'disabled'"/>
                </group>
            </group>
        </field>
    </record>
</odoo>
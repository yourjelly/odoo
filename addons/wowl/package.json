{
  "name": "wowl",
  "version": "0.0.1",
  "description": "Odoo Web Client",
  "main": "index.js",
  "scripts": {
    "build:js": "tsc",
    "build:bundle": "rollup -c",
    "build": "build:js && npm run build:bundle",
    "dev": "npm-run-all --parallel \"build:* -- --watch\"",
    "prettier": "prettier static/src --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "prettier": "^2.1.2",
    "typescript": "^4.0.3",
    "npm-run-all": "^4.1.5",
    "rollup": "^2.28.1"
  },
  "dependencies": {
    "@odoo/owl": "^1.0.7"
  },
  "prettier": {
    "printWidth": 100,
    "endOfLine": "auto"
  }
}

<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <div t-name="wowl.Notification" t-attf-class="show toast o_notification {{props.className}}"
         role="alert" aria-live="assertive" aria-atomic="true" owl="1">
        <t t-set="closeButton">
            <button type="button"
                    class="close o_notification_close"
                    data-dismiss="toast" aria-label="Close">
                <span class="d-inline" aria-hidden="true">&amp;times;</span>
            </button>
        </t>
        <div t-if="props.title" class="toast-header">
            <span t-attf-class="fa fa-2x mr-3 {{props.icon}} o_notification_icon" role="img"/>
            <div class="d-flex align-items-center mr-auto font-weight-bold o_notification_title"
                 t-raw="props.title"/>
            <t t-raw="closeButton"/>
        </div>
        <div class="toast-body d-flex">
            <strong t-if="props.subtitle" class="o_notification_subtitle" t-raw="props.subtitle"/>
            <div t-if="props.message" t-raw="props.message"
                 class="mr-auto o_notification_content"/>
            <div t-if="props.buttons.length" class="mt-2 o_notification_buttons">
                <button t-foreach="props.buttons" t-as="button" type="button"
                        t-attf-class="btn btn-sm {{button.primary ? 'btn-primary' : 'btn-secondary'}}">
                    <t t-if="button.icon">
                        <i t-if="button.icon.indexOf('fa-') === 0" role="img"
                            t-att-aria-label="button.name" t-att-title="button.name"
                            t-attf-class="fa fa-fw o_button_icon {{button.icon}}"/>
                        <img t-else="" t-att-src="button.icon" t-att-alt="button.name"/>
                    </t>
                    <span t-esc="button.text"/>
                </button>
            </div>
            <t t-if="!props.title" t-raw="closeButton"/>
        </div>
    </div>

</templates>

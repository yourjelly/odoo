<?xml version="1.0" encoding="utf-8"?>
    <odoo>
    <!-- Called by the plugin to open a lead in edit mode -->
        <record id="action_lead_edit_mode" model="ir.actions.act_window">
              <field name="name">Redirection to the lead edit form</field>
              <field name="type">ir.actions.act_window</field>
              <field name="res_model">crm.lead</field>
              <field name="view_mode">form</field>
              <field name="view_id" ref="crm.crm_lead_view_form"/>
              <field name="context">
                  {
                        'form_view_initial_mode': 'edit',
                  }
              </field>
        </record>
    </odoo>

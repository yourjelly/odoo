<odoo>
    <template id="portal_my_home_menu_sale" name="Portal layout : sales menu entries" inherit_id="portal.portal_layout" priority="20">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'quote' or sales_order and order.state in ('sent', 'cancel')">
                <a t-if="order" t-attf-href="/my/quotes?{{ keep_query() }}">Quotations</a>
                <t t-else="">Quotations</t>
            </li>
            <li t-if="page_name == 'order' or sales_order and order.state not in ('sent', 'cancel')">
                <a t-if="order" t-attf-href="/my/orders?{{ keep_query() }}">Sales Orders</a>
                <t t-else="">Sales Orders</t>
            </li>
            <li t-if="order">
                <t t-if="order.state in ('sent', 'cancel')">
                    <a href="/my/quotes?{{ keep_query() }}">Quotations / </a>
                    <t t-esc="order.name"/>
                </t>
                <t t-else="">
                    <a href="/my/orders?{{ keep_query() }}">Sales Orders / </a>
                    <t t-esc="order.name"/>
                </t>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_sale" name="Portal My Home : sales entries" inherit_id="portal.portal_my_home" priority="20">
        <xpath expr="//ul[hasclass('o_portal_docs')]" position="inside">
            <li t-if="quotation_count" class="list-group-item">
                <span class="badge" t-esc="quotation_count"/>
                <a href="/my/quotes">Quotations</a>
            </li>
            <li t-if="order_count" class="list-group-item">
                <span class="badge" t-esc="order_count"/>
                <a href="/my/orders">Sales Orders</a>
            </li>
        </xpath>
    </template>

    <template id="signature_dialog" name="signature Dialog">
        <div class="modal fade" id="modalaccept" role="dialog" aria-hidden="true">
            <div class="modal-dialog" t-if="portal_confirmation == 'sign'">
                <form id="accept" method="POST" t-att-data-order-id="quotation.id" t-att-data-token="quotation.access_token" class="js_accept_json modal-content js_website_submit_form">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&amp;times;</button>
                        <h4 class="modal-title">Accept &amp; Sign</h4>
                    </div>
                    <div class="modal-body" id="sign-dialog">
                        <p>
                            <span>I agree that by signing this proposal, I accept it on the behalf of </span>
                            <b t-field="quotation.partner_id.commercial_partner_id"/>
                            <span>, for an amount of </span>
                            <b data-id="total_amount" t-field="quotation.amount_total"
                                t-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'/>
                            <span>with payment terms: </span><b t-field="quotation.payment_term_id"/>.
                        </p>
                        <t t-if="portal_confirmation == 'sign'">
                            <t t-call="portal.portal_signature">
                                <t t-set="object" t-value="order"/>
                                <t t-set="partner_name" t-value="order.partner_id.name"/>
                                <t t-set="callUrl" t-value="'/my/quotes/accept'"/>
                                <t t-set="signLabel" t-value="'Accept Order'"/>
                                <t t-set="accessToken" t-value="quotation.access_token"/>
                            </t>
                        </t>
                    </div>
                </form>
            </div>
        </div>
        <div class="modal fade" id="modaldecline" role="dialog" aria-hidden="true">
            <div class="modal-dialog">
                <!-- TO DO : update form action  -->
                <form id="decline" method="POST" t-attf-action="/quote/#{quotation.id}/#{quotation.access_token}/decline" class="modal-content">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&amp;times;</button>
                        <h4 class="modal-title">Reject This Quote</h4>
                    </div>
                    <div class="modal-body">
                        <p>
                            Tell us why you are refusing this quotation, this will help us improve our services.
                        </p>
                        <textarea rows="4" name="decline_message" placeholder="Your feedback....." class="form-control"/>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" t-att-id="quotation.id" class="btn btn-primary">Reject</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                    </div>
                </form>
            </div>
        </div>
    </template>

    <template id="portal_my_quotations" name="My Quotations">
      <t t-call="portal.portal_layout">
        <h3>Quotations
            <t t-call="portal.portal_searchbar"/>
        </h3>
        <t t-if="not quotations">
            <p>There are currently no quotations for your account.</p>
        </t>
        <div t-if="quotations" class="panel panel-default">
            <div class="table-responsive"><table class="table table-hover o_portal_my_doc_table">
                <thead>
                  <tr class="active">
                      <th>Quotation #</th>
                      <th>Order Date</th>
                      <th>Valid Until</th>
                      <th></th>
                      <th>Total</th>
                    </tr>
                </thead>
                <t t-foreach="quotations" t-as="quotation">
                    <tr>
                        <td>
                            <a t-attf-href="/my/orders/#{quotation.id}?#{keep_query()}"><t t-esc="quotation.name"/></a>
                        </td>
                        <td><span t-field="quotation.date_order"/></td>
                        <td><span t-field="quotation.validity_date"/></td>
                        <td>
                          <t t-if="quotation.state == 'cancel'">
                            <span class="label label-default"><i class="fa fa-fw fa-remove"/> Cancelled</span>
                          </t>
                          <t t-if="quotation.is_expired">
                            <span class="label label-default"><i class="fa fa-fw fa-clock-o"/> Expired</span>
                          </t>
                        </td>
                        <td><span t-field="quotation.amount_total" t-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'/>
                        </td>
                    </tr>
                </t>
            </table></div>
            <div t-if="pager" class="o_portal_pager text-center">
              <t t-call="portal.pager"/>
            </div>
        </div>
      </t>
    </template>

    <template id="portal_my_orders" name="My Sales Orders">
       <t t-call="portal.portal_layout">
        <h3>Sales Orders
            <t t-call="portal.portal_searchbar"/>
        </h3>
        <t t-if="not orders">
            <p>There are currently no orders for your account.</p>
        </t>
        <div t-if="orders" class="panel panel-default">
            <div class="table-responsive"><table class="table table-hover o_portal_my_doc_table">
                <thead>
                  <tr class="active">
                    <th>
                      <span class='hidden-xs'>Sales Order #</span>
                      <span class='visible-xs'>Ref.</span>
                    </th>
                    <th>Order Date</th>
                    <th ></th>
                    <th>Total</th>
                  </tr>
                </thead>
                <t t-foreach="orders" t-as="order">
                    <tr>
                        <td>
                            <a t-attf-href="/my/orders/{{order.id}}?{{keep_query()}}"><t t-esc="order.name"/></a>
                        </td>
                        <td>
                          <span t-field="order.date_order" t-options="{'widget': 'date'}"/>&amp;nbsp;
                          <span class='hidden-xs' t-field="order.date_order" t-options="{'time_only': True}"/>
                        </td>
                        <td>
                            <t t-if="order.state == 'progress'">
                                <span class="label label-info"><i class="fa fa-fw fa-clock-o"></i><span class="hidden-xs"> Invoiced</span></span>
                            </t>
                            <t t-if="order.state in ['shipping_except','invoice_except']">
                                <span class="label label-danger"><i class="fa fa-fw fa-warning"></i><span class="hidden-xs"> Problem</span></span>
                            </t>
                            <t t-if="order.state == 'done'">
                                <span class="label label-default hidden-xs"><i class="fa fa-fw fa-check"></i><span class="hidden-xs"> Done</span></span>
                            </t>
                        </td>
                        <td><span t-field="order.amount_total" t-options='{"widget": "monetary", "display_currency": order.pricelist_id.currency_id}'/></td>
                    </tr>
                </t>
            </table></div>
            <div t-if="pager" class="o_portal_pager text-center">
              <t t-call="portal.pager"/>
            </div>
        </div>
     </t>
    </template>

    <template id="quote_expire_detail" name="Expiry details">
        <div t-if="order_valid and quotation.state in ['draft', 'sent']" class="text-center">
            <input type="hidden" t-att-value="quotation.validity_date" id="validity_date"/>
            <div class="mt8" t-if="days_valid &gt; 0" name="days_valid">
                <strong>This offer expires in</strong>
                 <div class="day_counter mt8 mb16">
                    <i class="fa fa-clock-o"></i>
                    <t t-esc="days_valid"/>
                    <t t-if="days_valid &gt; 1"> days</t>
                    <t t-if="days_valid &lt;= 1"> day</t>
                 </div>
            </div>
        </div>
    </template>

    <template id="confirmation_mode" name="quotation confirmation mode">
        <t t-if="order.state == 'sent'">
            <t t-call="sale.signature_dialog"/>
            <div class="text-center hidden-print mt16 mb16" id="quotation_toolbar">
                <a class="btn btn-success" data-toggle="modal" data-target="#modalaccept" t-if="portal_confirmation == 'sign'">
                    <i class="fa fa-arrow-circle-right"/> Accept &amp; Sign
                </a>
                <a class="btn btn-info" type="submit" href="#discussion">
                    <i class="fa fa-comment"/> Feedback
                </a>
                <a class="btn btn-danger" data-toggle="modal" data-target="#modaldecline">
                    <i class="fa fa-times"/> Reject
                </a>
            </div>
        </t>
    </template>

    <template id="pricing" name="Price">
        <section id="quote">
            <h1 class="page-header">Pricing</h1>
            <t t-foreach="quotation.order_lines_layouted()" t-as="page">
              <table class="table wq-table">
                  <thead>
                      <tr>
                          <th>Products</th>
                          <th>Quantity</th>
                          <th><span t-if="any([line.tax_id for line in quotation.order_line])">Taxes</span></th>
                          <th><span t-if="True in [line.discount > 0 for line in quotation.order_line]">Discount</span></th>
                          <th class="text-right">Unit Price</th>
                          <th class="text-right" groups="sale.group_show_price_subtotal">Amount</th>
                          <th class="text-right price_tax_included" groups="sale.group_show_price_total">Total Price</th>
                          <th></th>
                      </tr>
                  </thead>
                  <tbody>
                      <t t-foreach="page" t-as="layout_category">
                          <t t-if="layout_category_size > 1 or page_size > 1" groups="sale.group_sale_layout">
                              <tr class="active">
                                  <td colspan="7" style="font-weight: bold; border-bottom: 1px solid black;">&amp;bull;
                                      <t t-esc="layout_category['name']"/>
                                  </td>
                              </tr>
                          </t>
                          <!-- Lines associated -->
                          <t t-foreach="layout_category['lines']" t-as="line">
                              <tr>
                                <td id="product_name">
                                     <span t-field="line.name"/>
                                </td>
                                <td>
                                    <div id="quote_qty">
                                        <span t-field="line.product_uom_qty"/>
                                        <span t-field="line.product_uom"/>
                                    </div>
                                </td>
                                <td>
                                    <div t-foreach="line.tax_id" t-as="tax">
                                        <t t-esc="tax.name"/>
                                    </div>
                                </td>
                                <td>
                                    <strong t-if="line.discount" class="text-info">
                                        <t t-esc="((line.discount % 1) and '%s' or '%d') % line.discount"/>% discount
                                    </strong>
                                </td>
                                <td class="text-right">
                                      <div t-field="line.price_unit"
                                          t-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'
                                          t-att-style="line.discount and 'text-decoration: line-through' or None"
                                          t-att-class="(line.discount and 'text-danger' or '') + ' text-right'"/>
                                      <div t-if="line.discount">
                                          <t t-esc="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'/>
                                      </div>
                                </td>
                                <td class="text-right" groups="sale.group_show_price_subtotal">
                                    <span t-field="line.price_subtotal"
                                          t-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'/>
                                </td>
                                <td class="text-right" groups="sale.group_show_price_total">
                                    <span t-field="line.price_total"
                                          t-options='{"widget": "monetary", "display_currency": quotation.pricelist_id.currency_id}'/>
                                </td>
                            </tr>
                          </t>

                          <t t-if="(layout_category_size > 1  or page_size > 1) and layout_category['subtotal']" groups="sale.group_sale_layout">
                              <tr>
                                  <td></td><td></td><td></td><td></td>
                                  <td class="text-right" style="padding-bottom: 32px"><strong>Subtotal:</strong></td>
                                  <td class="text-right" style="padding-bottom: 32px">
                                      <t t-set="subtotal" t-value="sum(line.price_subtotal for line in layout_category['lines'])"/>
                                      <strong data-id="total_amount" t-esc="subtotal" t-options="{'widget': 'monetary', 'display_currency': quotation.pricelist_id.currency_id}"/>
                                  </td>
                                  <td></td>
                              </tr>
                          </t>
                      </t>
                      <t t-if="page_index == page_size - 1">
                          <t t-if="any([line.tax_id for line in quotation.order_line])">
                              <tr>
                                  <td></td><td></td><td></td><td></td>
                                  <td class="text-right"><strong>Subtotal:</strong></td>
                                  <td class="text-right">
                                      <strong data-id="total_amount" t-field="quotation.amount_untaxed" t-options='{"widget": "monetary","display_currency": quotation.pricelist_id.currency_id}'/>
                                  </td>
                                  <td></td>
                              </tr>

                              <tr>
                                  <td></td><td></td><td></td><td></td>
                                  <td class="text-right">Taxes:</td>
                                  <td class="text-right">
                                      <span data-id="total_amount" t-field="quotation.amount_tax" t-options='{"widget": "monetary","display_currency": quotation.pricelist_id.currency_id}'/>
                                  </td>
                                  <td></td>
                              </tr>
                          </t>
                          <tr>
                              <td></td><td></td><td></td><td></td>
                              <td class="text-right"><strong>Total:</strong></td>
                              <td class="text-right">
                                  <strong data-id="total_amount" t-field="quotation.amount_total" t-options='{"widget": "monetary","display_currency": quotation.pricelist_id.currency_id}'/>
                              </td>
                              <td></td>
                          </tr>
                      </t>
                  </tbody>
              </table>

              <t t-if="page_index &lt; page_size - 1" groups="sale.group_sale_layout">
                  <p style="page-break-before:always;"> </p>
              </t>
            </t>
        </section>
        <section id="terms" class="container" t-if="quotation.note">
            <h2 class="page-header">Terms &amp; Conditions</h2>
            <p t-field="quotation.note"/>
        </section>
    </template>

    <template id="quotation_content" name="sale order content">
        <div id="introduction" class="page-header mt0">
            <h1>
                <t t-if="not (env.context.get('proforma', False) or is_pro_forma)">
                    <span t-if="order.state in ('draft','sent','cancel')">Your Quotation</span>
                    <span t-if="order.state not in ('draft','sent','cancel')">Your Order</span>
                </t>
                <t t-if="env.context.get('proforma', False) or is_pro_forma">
                    <span>Pro-Forma Invoice # </span>
                </t>
                <em t-esc="order.name"/>
                <small t-field="order.state"/>
                <div t-ignore="true" class="pull-right css_editable_mode_hidden hidden-print">
                    <ul class="list-inline">
                        <li t-if="portal_layout">
                            <a target="_blank" t-att-href="'/my/orders/pdf/%s?%s' % (order.id, keep_query())" class="btn btn-info" title="Download">
                                Print
                            </a>
                        </li>
                        <li>
                            <t t-call='portal.record_pager'/>
                        </li>
                    </ul>
                </div>
            </h1>
        </div>
        <div class="row mt32 o_customer">
            <div class="col-md-6 col-xs-6">
                <div class="row">
                    <label class="col-sm-4 col-xs-4 text-right">Customer:</label>
                    <div class="col-sm-8 col-xs-8">
                        <div t-field="order.partner_id"/>
                    </div>
                </div>
                <div class="row">
                    <label class="col-sm-4 col-xs-4 text-right">Bill To:</label>
                    <div class="col-sm-8 col-xs-8">
                        <div t-field="order.partner_invoice_id" t-options='{
                            "widget": "contact",
                            "fields": ["address", "name", "phone", "email"]
                            }'/>
                    </div>
                </div>
                <div t-if="order.partner_shipping_id.id != order.partner_invoice_id.id" class="row" id="shipping_address">
                    <label class="col-sm-4 col-xs-4 text-right">Ship To:</label>
                    <div class="col-sm-8 col-xs-8">
                        <div t-field="order.partner_shipping_id" t-options='{
                          "widget": "contact",
                          "fields": ["address", "name", "phone"]
                          }'/>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-xs-6">
                <div class="row">
                    <label class="col-sm-5 col-xs-5 text-right">Your Contact:</label>
                    <div class="col-sm-7 col-xs-7">
                        <div t-field="order.user_id" t-options='{
                            "widget": "contact",
                            "fields": ["name", "phone", "email"]
                            }'/>
                    </div>
                </div>
                <div class="row">
                    <label class="col-sm-5 col-xs-5 text-right">Quote Date:</label>
                    <div class="col-sm-7 col-xs-7">
                        <span t-field="order.date_order"/>
                    </div>
                    <div class="clearfix"/>
                    <div t-if="order.validity_date and order.state in ['draft', 'sent']">
                      <label class="col-sm-5 col-xs-5 text-right">Expiration Date:</label>
                      <div class="col-sm-7 col-xs-7">
                          <span t-field="order.validity_date"/>
                      </div>
                    </div>
                    <div class="clearfix"/>
                    <div t-if="order.client_order_ref">
                        <label class="col-sm-5 col-xs-5 text-right">Your Reference:</label>
                        <div class="col-sm-7 col-xs-7">
                            <span t-field="order.client_order_ref"/>
                        </div>
                    </div>
                    <div class="clearfix"/>
                    <div t-if="order.payment_term_id">
                        <label class="col-sm-5 col-xs-5 text-right">Payment Terms:</label>
                        <div class="col-sm-7 col-xs-7">
                            <span t-field="order.payment_term_id"/>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="oe_structure"/>
        <!-- Pricing -->
        <div id="so_pricing">
            <t t-call="sale.pricing" t-if="order.order_line">
                <t t-set="quotation" t-value="order"/>
            </t>
        </div>
        <!-- quotation confirmation mode -->
        <div id="confirmation_mode">
            <t t-call="sale.confirmation_mode">
                <t t-set="quotation" t-value="order"/>
            </t>
        </div>
        <!-- History -->
        <div id="history" class="hidden-print">
            <t t-call="sale.chatter"/>
        </div>
    </template>

    <template id="portal_order_page" name="Sales Order" inherit_id="portal.portal_sidebar" primary="True">
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <div class="row mt16">
                <!-- Sidebar -->
                <div class="col-md-3 hidden-print">
                    <div class="bs-sidebar" id="sidebar_content">
                        <div class="text-center o_sale_portal_sidebar">
                            <h2><strong>
                                <span t-field="order.amount_total" t-options='{"widget": "monetary", "display_currency": order.pricelist_id.currency_id}'/>
                            </strong></h2>
                        </div>
                        <div class="text-center visible-xs visible-sm" style="padding: 10px">
                            <a class="btn btn-primary" target="_blank" t-att-href="'/my/orders/pdf/%s?%s' % (order.id, keep_query())" title="Download">Dowanload</a>
                        </div>
                        <hr class="mt0 mb0" t-if="order.state == 'sent' and portal_confirmation == 'sign'"/>
                        <div class="mt4" id="confirmation_button">
                            <div class="o_quote_button ml16 mr16 mb8">
                                <t t-if="order.state == 'sent' and portal_confirmation == 'sign'" name="portal_confirmation_sign">
                                    <a class="btn btn-primary btn-block" data-toggle="modal" data-target="#modalaccept">
                                        <i class="fa fa-arrow-circle-right"/> Accept &amp; Sign
                                    </a>
                                </t>
                                <t t-if="order.state == 'sale' and portal_confirmation == 'sign'">
                                    <a class="btn btn-success btn-block" disabled="disabled" t-attf-title="Order signed on #{order.confirmation_date} by #{order.partner_id.name}">
                                        <i class="fa fa-check-circle"/> Signed
                                    </a>
                                </t>
                                <a t-if="not order_valid" href="#discussion" class="btn btn-info btn-block o_normalwhitespace">
                                    <strong>This offer expired!</strong><br/>
                                    Contact us for new quote.
                                </a>
                                <div class="text-center mt8" t-if="order_valid">
                                    <a type="submit" href="#discussion">
                                        Ask Changes
                                    </a>
                                    <a data-toggle="modal" data-target="#modaldecline">
                                        Reject
                                    </a>
                                </div>
                            </div>
                        </div>
                        <hr class="mt0 mb0"/>
                        <div class="navspy" t-ignore="true" role="complementary">
                            <ul class="nav bs-sidenav" data-id="portal_sidebar">
                                <li><a href="#introduction">Introduction</a></li>
                            </ul>
                        </div>
                        <hr class="mt0 mb0"/>
                        <div class="o_expiry_detail">
                            <t t-call="sale.quote_expire_detail">
                                <t t-set="quotation" t-value="order"/>
                            </t>
                        </div>
                    </div>
                </div>
                <div id="page_content" class="col-md-9 hidden-print">
                    <t t-if="error or warning" t-call="sale.portal_order_error"/>
                    <t t-if="success and (not error and not warning)" t-call="sale.portal_order_success"/>
                    <t t-call="sale.quotation_content"/>
                </div>
            </div>
        </xpath>
    </template>

    <template id="chatter" name="Allow Comments &amp; History">
        <h1 class="page-header" id="chatter">History</h1>
        <!-- Options:Quotation Chatter: user can reply -->
        <t t-call="portal.message_thread">
            <t t-set="object" t-value="order"/>
        </t>
    </template>

    <template id="portal_order_error" name="Order error/warning display">
        <div class="row">
            <div t-att-class="'col-md-6 col-md-offset-3 text-center alert alert-dismissable %s' % ('alert-danger' if error else 'alert-warning')">
                <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
                <t t-if="error == 'generic'" name="generic">
                    There was an error processing this page.
                </t>
            </div>
        </div>
    </template>

    <template id="portal_order_success" name="Order success display">
        <div class="row">
            <div class="col-md-6 col-md-offset-3 text-center alert alert-dismissable alert-success">
                <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
            </div>
        </div>
    </template>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_partner_sales_form_inherited" model="ir.ui.view">
        <field name="name">res.partner.form.inherited</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="check_for_users_sale"/>
                <field name="sale_order_creation_responsible_ids" widget="many2many_tags" invisible="not check_for_users_sale"/>
                <field name="sale_order_approval_responsible_ids" widget="many2many_tags" invisible="not check_for_users_sale"/>
            </xpath>
        </field>
    </record>
</odoo>
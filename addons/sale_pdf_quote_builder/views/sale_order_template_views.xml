<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="sale_order_template_form" model="ir.ui.view">
        <field name="name">sale.order.template.form</field>
        <field name="model">sale.order.template</field>
        <field name="inherit_id" ref="sale_management.sale_order_template_view_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page name="pdf_quote" string="PDF Quote">
                    <group>
                        <group>
                            <field name="sale_header"/>
                            <field name="sale_footer"/>
                        </group>
                        <group>
                            <p class="text-muted" colspan="2">
                                Set a PDF header and footer to produce a high quality quotation, built
                                by concatenating the template header, product sheets, the quotation with
                                product and prices, and the footer.
                            </p><p class="text-muted" colspan="2">
                                To include product sheets in your Quotation templates, add documents on
                                product forms, and set their visibility as "Inside Quotes".
                            </p>
                        </group>
                    </group>
                </page>
            </notebook>
        </field>
    </record>

</odoo>

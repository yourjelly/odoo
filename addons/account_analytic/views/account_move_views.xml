<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- account.move.line (Journal items) -->
        <record id="view_move_line_form" model="ir.ui.view">
            <field name="name">account.analytic.move.line.form</field>
            <field name="model">account.move.line</field>
            <field name="inherit_id" ref="account.view_move_line_form"/>
            <field eval="2" name="priority"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='information']/group/group[last()]" position="after">
                    <group string="Analytic" groups="analytic.group_analytic_accounting,analytic.group_analytic_tags">
                        <field name="analytic_account_id" groups="analytic.group_analytic_accounting"
                            domain="['|', ('company_id', '=', company_id), ('company_id', '=', False)]"
                            attrs="{'readonly':[('parent_state','=','posted')]}"/>
                        <field name="analytic_tag_ids" groups="analytic.group_analytic_tags"
                            widget="many2many_tags"/>
                    </group>
                </xpath>
                <xpath expr="//page[@name='information']" position="after">
                    <page string="Analytic Lines" name="analytic_lines" groups="analytic.group_analytic_accounting">
                        <field name="date" invisible="1"/>
                        <field name="analytic_line_ids"
                            context="{'tree_view_ref':'analytic.view_account_analytic_line_tree', 'default_general_account_id':account_id, 'default_name': name, 'default_date':date, 'amount': (debit or 0.0)-(credit or 0.0)}"/>
                    </page>
                </xpath>
            </field>
        </record>

        <record id="view_move_line_tree" model="ir.ui.view">
            <field name="name">account.analytic.move.line.tree</field>
            <field name="model">account.move.line</field>
            <field name="inherit_id" ref="account.view_move_line_tree"/>
            <field eval="100" name="priority"/>
            <field name="arch" type="xml">
                <field name="name" optional="after">
                    <field name="analytic_account_id" groups="account.group_account_readonly" optional="show"/>
                    <field name="analytic_tag_ids" widget="many2many_tags" groups="analytic.group_analytic_tags" optional="hide"/>
                </field>
            </field>
        </record>

        <record id="view_move_line_tax_audit_tree" model="ir.ui.view">
            <field name="name">account.analytic.move.line.tax.audit.tree</field>
            <field name="model">account.move.line</field>
            <field name="mode">primary</field>
            <field name="inherit_id" ref="account.view_move_line_tree"/>
            <field name="arch" type="xml">
                <field name="analytic_account_id" position="replace"/>
            </field>
        </record>

        <record id="view_move_form" model="ir.ui.view">
            <field name="name">account.analytic.move.form</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <!-- invoice tab page > tree -->
                <xpath expr="//page[@id='invoice_tab']//tree//field[@name='account_id']" position="after">
                    <field name="analytic_account_id"
                            domain="['|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]"
                            groups="analytic.group_analytic_accounting"
                            optional="show"/>
                    <field name="analytic_tag_ids"
                            domain="['|', ('company_id', '=', False), ('company_id', '=', parent.company_id)]"
                            groups="analytic.group_analytic_tags"
                            optional="show"
                            widget="many2many_tags"/>
                </xpath>
                <!-- invoice tab page > form -->
                <xpath expr="//page[@id='invoice_tab']//form//field[@name='account_id']" position="before">
                    <field name="analytic_tag_ids" groups="analytic.group_analytic_tags" widget="many2many_tags"/>
                </xpath>
                <xpath expr="//page[@id='invoice_tab']//form//field[@name='tax_ids']" position="after">
                    <field name="analytic_account_id" groups="analytic.group_analytic_accounting"/>
                </xpath>
                <!-- aml tab page > tree -->
                <xpath expr="//page[@id='aml_tab']//tree//field[@name='name']" position="after">
                    <field name="analytic_account_id"
                            optional="hide"
                            domain="['|', ('company_id', '=', parent.company_id), ('company_id', '=', False)]"
                            groups="analytic.group_analytic_accounting"
                            attrs="{'invisible': [('display_type', 'in', ('line_section', 'line_note'))]}"/>
                    <field name="analytic_tag_ids"
                            optional="show"
                            groups="analytic.group_analytic_tags"
                            widget="many2many_tags"
                            attrs="{'invisible': [('display_type', 'in', ('line_section', 'line_note'))]}"/>
                </xpath>
                <!-- aml tab page > form -->
                <xpath expr="//page[@id='aml_tab']//form//field[@name='name']" position="after">
                    <field name="analytic_account_id" groups="analytic.group_analytic_accounting"/>
                    <field name="analytic_tag_ids" groups="analytic.group_analytic_tags" widget="many2many_tags"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>

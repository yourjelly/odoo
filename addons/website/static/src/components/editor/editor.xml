<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="website.WebsiteEditorComponent" owl="1">
        <t t-if="this.state.edition">
            <WysiwygAdapterComponent Component="this.Wysiwyg" iframe="this.iframe"
                                    target="this.reload_target"
                                    reloadCallback="(event, widgetEl) => this.reload(event, widgetEl)"
                                    quitCallback="() => this.quit()"/>
        </t>
        <div t-ref="loading">
            <t t-if="this.state.loading">
                <t t-out="this.loadingDummy" />
                <!-- coulnd't use o_we_ui_loading here because wysiwys_snippets style might not be loaded-->
                <div class="o_website_loading d-flex justify-content-center align-items-center">
                    <i class="fa fa-circle-o-notch fa-spin fa-4x" />
                </div>
            </t>
        </div>
    </t>
    <div t-name="website.homepage_editor_welcome_message" class="container text-center o_homepage_editor_welcome_message">
        <h2 class="mt0">Welcome to your <b>Homepage</b>!</h2>
        <p class="lead d-none d-md-block">Click on <b>Edit</b> in the top right corner to start designing.</p>
    </div>
</templates>

odoo.define('website.s_ras_options', function (reauire) {
'use strict';

const options = require('web_editor.snippets.options');

   options.registry.SnipetCounter = options.Class.extend({
    // isTopOption: true,
    start: function () {
        debugger;
    },
    
});
});
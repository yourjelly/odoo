<?xml version="1.0" ?>
<odoo>
    <data noupdate="1">
        <record id="sms_template_data_point_of_sale" model="sms.template">
            <field name="name">POS:Sent Order Confirmation via Text</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="body">
                {{ (object.company_id.name + ': your order ' + object.order_number + 'rs' +
                    object.order_amount + 'is processed succesfully. Use' + object_order_number + 'for further reference')}}
            </field>
        </record>
    </data>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_company_form" model="ir.ui.view">
        <field name="name">res.company.form.inherit.account</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='country_id']" position="after">
                <field name="country_code" invisible="1"/>
            </xpath>

            <xpath expr="//page[@name='general_info']" position='after'>
                <page string="VAT" name="account_vat_numbers">
                    <group>
                        <group>
                            <field name="vat_number_ids" nolabel="1"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <record id="company_vat_tree" model="ir.ui.view">
        <field name="name">account.company.vat.tree</field>
        <field name="model">account.company.vat</field>
        <field name="arch" type="xml">
            <tree editable="bottom"> <!--TODO OCO ordonner par nom de pays ?-->
                <field name="country_id"/>
                <field name="vat"/>
            </tree>
        </field>
    </record>
</odoo>

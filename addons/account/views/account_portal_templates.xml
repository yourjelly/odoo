<odoo>
	<template id="portal_my_home_menu_invoice" name="Portal layout : invoice menu entries" inherit_id="portal.portal_layout" priority="30">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'invoice'" class="active">
                <a t-if="invoice" t-attf-href="/my/invoices?{{ keep_query() }}">Invoices</a>
                <t t-else="">Invoices</t>
            </li>
            <li t-if="invoice">
                <t t-esc="invoice.number"/>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_invoice" name="Portal My Home : invoice entries" inherit_id="portal.portal_my_home" priority="30">
        <xpath expr="//ul[hasclass('o_portal_docs')]" position="inside">
            <li t-if="invoice_count" class="list-group-item">
                <span class="badge" t-esc="invoice_count"/>
                <a href="/my/invoices">Invoices</a>
            </li>
        </xpath>
    </template>

    <template id="portal_my_invoices" name="My Invoices and Payments">
      <t t-call="portal.portal_layout">
        <h3>Invoices
            <t t-call="portal.portal_searchbar"/>
        </h3>
        <t t-if="not invoices">
            <p>There are currently no invoices and payments for your account.</p>
        </t>
        <div t-if="invoices" class="panel panel-default">
            <div class="table-responsive"><table class="table table-hover o_portal_my_doc_table">
                <thead>
                  <tr class="active">
                    <th>Invoice #</th>
                    <th>Invoice Date</th>
                    <th class='hidden-xs'>Due Date</th>
                    <th></th>
                    <th>Amount Due</th>
                  </tr>
                </thead>
                <t t-foreach="invoices" t-as="invoice">
                    <tr>
                        <td>
                            <a t-attf-href="/my/invoices/#{invoice.id}" t-att-title="invoice.number"><t t-esc="invoice.number"/></a>
                        </td>
                        <td><span t-field="invoice.date_invoice"/></td>
                        <td class='hidden-xs'><span t-field="invoice.date_due"/></td>
                        <td>
                            <t t-if="invoice.state == 'open'">
                                <span class="label label-info"><i class="fa fa-fw fa-clock-o"></i><span class="hidden-xs"> Waiting for Payment</span></span>
                            </t>
                            <t t-if="invoice.state == 'paid'">
                                <span class="label label-default"><i class="fa fa-fw fa-check"></i><span class="hidden-xs"> Paid</span></span>
                            </t>
                            <t t-if="invoice.state == 'cancel'">
                                <span class="label label-default"><i class="fa fa-fw fa-remove"></i><span class="hidden-xs"> Cancelled</span></span>
                            </t>
                        </td>
                        <td><span t-field="invoice.residual" t-options='{"widget": "monetary", "display_currency": invoice.currency_id}'/></td>
                    </tr>
                </t>
            </table></div>
            <div t-if="pager" class="o_portal_pager text-center">
                <t t-call="portal.pager"/>
            </div>
        </div>
      </t>
    </template>

    <template id="portal_invoice_page" name="Invoice Portal Template" inherit_id="portal.portal_sidebar" primary="True">
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <div class="row mt16">
                <!-- Sidebar -->
                <div class="col-md-3 col-sm-4 hidden-print">
                    <div class="bs-sidebar" id="sidebar_content">
                        <div class="text-center o_account_portal_sidebar">
                            <h2><strong><span t-field="invoice.amount_total"/></strong></h2>
                            <div class="mb4" t-if="invoice.state != 'paid'"><i class="fa fa-clock-o"/><span class="o_timeago ml4" t-att-datetime="invoice.date_due"/></div>
                        </div>
                        <div class="o_payment_button">
                            <div class="o_download_pdf text-center mb8 ml16 mr16 mb8">
                                <a t-att-href="'/my/invoices/pdf/%s?%s' % (invoice.id, keep_query())" title="Download"><i class="fa fa-download"/> Download</a>
                                <a class="ml4 hidden-xs hidden-sm" href="#" id="print_invoice_report" title="Print"><i class="fa fa-print"/> Print</a>
                            </div>
                        </div>
                        <t t-if="invoice.user_id">
                            <hr class="mt0 mb0"/>
                            <div class="mb4 ml16">
                                <strong>Your Salesperson:</strong>
                                <ul class="o_portal_salesperson">
                                    <li>
                                        <img class="img-circle mr4 pull-left" t-if="invoice.user_id.image" t-attf-src="data:image/png;base64,#{invoice.user_id.image}" width="40px"/>
                                        <img class="img-circle mr4 pull-left" t-if="not invoice.user_id.image" src="/web/static/src/img/placeholder.png" width="40px"/>
                                        <span t-field="invoice.user_id" t-field-options='{"widget": "contact", "fields": ["name", "phone"], "no_marker": True}' />
                                    </li>
                                </ul>
                            </div>
                        </t>
                        <div id="History" class="ml16 mb8">
                            <a data-toggle="modal" href="#" data-target="#invoice_chatter">A question about the Invoice?</a>
                        </div>
                    </div>
                </div>
                <div id="page_content" class="col-md-9 col-sm-8 o_portal_page_content hidden-print">
                    <t t-if="error or warning" t-call="account.portal_invoice_error"/>
                    <t t-if="success and (not error and not warning)" t-call="account.portal_invoice_success"/>
                    <div class="o_portal_html_view">
                        <iframe id="invoice_html" class="mt4" width="100%" height="100%" frameborder="0" scrolling="no" t-attf-src="/my/invoices/html/#{invoice.id}/#{invoice.access_token}" />
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="chatter" name="Allow Comments &amp; History" inherit_id="account.portal_invoice_page">
        <xpath expr="//div[@id='page_content']//div[hasclass('o_portal_html_view')]" position="after">
            <div class="modal fade" id="invoice_chatter" role="dialog" aria-hidden="true">
                <div class="modal-dialog modal-content">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&amp;times;</button>
                            <h3 class="page-header">History</h3>
                        </div>
                        <div class="modal-body">
                            <t t-call="portal.message_thread">
                                <t t-set="object" t-value="invoice"/>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="portal_invoice_error" name="Invoice error/warning display">
        <div class="row mr16">
            <div t-attf-class="'col-md-12 mr16 ml16 alert alert-dismissable' #{'alert-danger' if error else 'alert-warning'}">
                <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
                <t t-if="error == 'generic'" name="generic">
                    There was an error processing this page.
                </t>
            </div>
        </div>
    </template>

    <template id="portal_invoice_success" name="Invoice success display">
        <div class="row mr16">
            <div class="col-md-12 mr16 ml16 alert alert-dismissable alert-success">
                <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
            </div>
        </div>
    </template>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--
        Procurement
        -->

        <record id="procurement_group_form_view" model="ir.ui.view">
            <field name="name">procurement.group.form</field>
            <field name="model">procurement.group</field>
            <field name="arch" type="xml">
                <form string="Procurement group">
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="%(do_view_pickings)d" string="Pickings" type="action" class="oe_stat_button" icon="fa-truck"/>
                        </div>
                        <group>
                            <field name="name"/>
                            <field name="move_type"/>
                        </group>                        
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Procurement Rules -->

        <record id="view_procurement_rule_filter" model="ir.ui.view">
            <field name="name">procurement.rule.select</field>
            <field name="model">procurement.rule</field>
            <field name="arch" type="xml">
                <search string="Search Procurement">
                    <filter string="Global" name="route_false" domain="[('route_id','=',False)]" help="If the route is global"/>
                    <separator/>
                    <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
                </search>
            </field>
        </record>

        <!-- <Rules> -->
        <record id="view_procurement_rule_tree2" model="ir.ui.view">
            <field name="name">procurement.rule.tree2</field>
            <field name="model">procurement.rule</field>
            <field name="arch" type="xml">
                <tree string="Rules">
                    <field name="sequence" widget="handle"/>
                    <field name="action"/>
                    <field name="location_src_id"/>
                    <field name="location_id"/>
                    <field name="route_id"/>
                </tree>
            </field>
        </record>

        <record id="view_procurement_rule_form" model="ir.ui.view">
            <field name="name">procurement.rule.form</field>
            <field name="model">procurement.rule</field>
            <field name="arch" type="xml">
                <form string="Rules">
                    <sheet>
                        <div class="oe_title">
                            <label for="name" class="oe_edit_only"/>
                            <h1><field name="name"/></h1>
                        <group>
                            <field name="sequence" string="Sequence" groups="base.group_no_one"/>
                        </group>
                        </div>
                        <group>
                            <group>
                                <field name="action"/>
                                <field name="location_src_id"/>
                                <field name="location_id"/>
                                <field name="procure_method" attrs="{'invisible': [('action', 'in', ['push', 'buy', 'manufacture'])]}"/>
                                <field name="auto" attrs="{'invisible': [('action', 'in', ['buy', 'move', 'manufacture'])]}"/>
                                <field name="picking_type_id"/>
                            </group>
                            <group>
                                <field name="warehouse_id" attrs="{'invisible': [('action', '=', 'push')]}"/>
                                <field name="route_id"/>
                            </group>
                        </group>
                        <group>
                            <group string="Options" attrs="{'invisible': [('action', 'in', ['buy', 'manufacture'])]}">
                                <field name="partner_address_id" attrs="{'invisible': [('action', '=', 'push')]}"/>
                                <field name="delay"/>
                            </group>
                            <group name="propagation_group" string="Propagation" attrs="{'invisible': [('action', '=', 'push')]}" groups="base.group_no_one">
                                <field name="group_propagation_option"/>
                                <field name="group_id" attrs="{'invisible': [('group_propagation_option', '!=', 'fixed')], 'required': [('group_propagation_option', '=', 'fixed')]}"/>
                                <field name="propagate"/>
                                <field name="propagate_warehouse_id"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="action_rules_form" model="ir.actions.act_window">
            <field name="name">Rules</field>
            <field name="res_model">procurement.rule</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
        </record>

        <menuitem action="action_rules_form" id="menu_action_rules_form"
        parent="menu_warehouse_config" sequence="2" groups="stock.group_stock_multi_locations"/>

    </data>
</odoo>

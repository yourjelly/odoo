<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="project_todo.TodoFormView" t-inherit="web.FormView" t-inherit-mode="primary">
        <xpath expr="//div[hasclass('o_form_view_container')]/Layout" position="inside">
            <t t-set-slot="control-panel-priority-field">
                <t t-set="record" t-value="model.root"/>
                <div class="o_field_widget mb-0">
                    <PriorityField
                        name="'priority'"
                        record="record"
                        id="'priority'"
                    />
                </div>
            </t>
            <t t-set-slot="control-panel-todo-name">
                <t t-set="record" t-value="model.root"/>
                <TodoEditableBreadcrumbName
                    name="'name'"
                    record="record"
                    id="'name'"
                />
            </t>
            <t t-set-slot="todo-mark-as-done-button">
                <t t-set="record" t-value="model.root"/>
                <TodoDoneCheckmark
                    name="'state'"
                    record="record"
                    id="'state'"
                    viewType="'form'"
                />
            </t>
            <t t-set-slot="todo-tag-ids">
                <t t-set="record" t-value="model.root"/>
                <TodoTagIds
                    name="'tag_ids'"
                    record="record"
                    id="'tag_ids'"
                />
            </t>
            <t t-set-slot="todo-user-ids">
                <t t-set="record" t-value="model.root"/>
                <TodoUserIds
                    name="'user_ids'"
                    record="record"
                    id="'user_ids'"
                />
            </t>
            <t t-set-slot="todo-personal-stage">
                <t t-set="record" t-value="model.root"/>
                <StatusBarField 
                    name="'personal_stage_type_id'"
                    record="record"
                    id="'personal_stage_type_id'"
                />
            </t>
            <t t-set-slot="todo-chatter-panel">
                <t t-set="record" t-value="model.root"/>
                <TodoChatterPanel
                    record="record"
                />
            </t>
        </xpath>
    </t>

</templates>

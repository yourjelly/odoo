<?xml version="1.0"?>
<odoo>
    <function model="mail.message" name="create">
        <value model="mail.message" eval="dict({
            'model':'res.partner',
            'res_id':obj().env['res.partner'].search([]),
            'body':'Contact created',
            'record_name': False,
            })
            "/>
    </function>
</odoo>

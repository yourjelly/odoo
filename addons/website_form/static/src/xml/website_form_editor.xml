<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!-- End Message -->
    <t t-name="website_form.s_website_form.end_message">
        <div class="s_website_form_end_message d-none">
            <div class="text-center alert alert-info css_non_editable_mode_hidden o_not_editable" t-ignore="True" role="status">
                The following message will become visible <strong>only</strong> once the form is sent.
            </div>
            <div class="oe_structure">
                <section class="s_picture bg-200">
                    <div class="container">
                        <div class="row">
                            <div class="col-12 pt16">
                                <h2 class="text-center">Thank You For Your Feedback</h2>
                                <p class="text-center">
                                    Our team will message you back as soon as possible.<br/>
                                    In the meantime we invite you to try our awesome <a href="/">website</a>.<br/>
                                </p>
                            </div>
                            <div class="col-lg-8 offset-lg-2">
                                <figure class="figure">
                                    <img src="/web/image/website.library_image_18" class="figure-img img-fluid rounded img-thumbnail padding-large" alt="Countdown is over - Firework"/>
                                </figure>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </t>

    <!-- Generic Field Layout -->
    <t t-name="website_form.field">
        <div t-att-class="'form-group col-12 form-field ' + (field.custom ? ' o_website_form_custom' : '') + (field.required ? ' o_website_form_required_custom' : '') + (field.hidden ? ' o_website_form_field_hidden' : '')"
            t-att-data-type="field.custom and field.type">
            <div t-if="field.formatInfo.labelPosition == 'left' || field.formatInfo.labelPosition == 'right'" class="row s_col_no_resize s_col_no_bgcolor">
                <label t-attf-class="col-form-label col-auto o_we_form_label #{field.formatInfo.labelPosition == 'right' ? 'text-right' : ''}" t-att-style="'width: ' + field.formatInfo.labelWidth" t-att-for="field.name">
                    <t t-esc="field.string"/>
                </label>
                <div class="col">
                    <t t-raw="0"/>
                </div>
            </div>
            <t t-else="">
                <label t-attf-class="o_we_form_label #{field.formatInfo.labelPosition == 'none' ? 'd-none' : ''}" t-att-style="'width: ' + field.formatInfo.labelWidth" t-att-for="field.name">
                    <t t-esc="field.string"/>
                </label>
                <t t-raw="0"/>
            </t>
        </div>
    </t>

    <!-- Char Field -->
    <t t-name="website_form.field_char">
        <t t-call="website_form.field">
            <input
                type="text"
                class="form-control o_website_form_input"
                t-att-name="field.name"
                t-att-required="field.required"
                t-att-value="field.value"
            />
        </t>
    </t>

    <!-- Text Field -->
    <t t-name="website_form.field_text">
        <t t-call="website_form.field">
            <textarea
                class="form-control o_website_form_input"
                t-att-name="field.name"
                t-att-required="field.required"
            />
        </t>
    </t>

    <!-- Email Field -->
    <t t-name="website_form.field_email">
        <t t-call="website_form.field">
            <input
                type="email"
                class="form-control o_website_form_input"
                t-att-name="field.name"
                t-att-required="field.required"
            />
        </t>
    </t>

    <!-- HTML Field -->
    <t t-name="website_form.field_html">
        <!--
            Maybe use web_editor ? Not sure it actually makes
            sense to have random people editing html in a form...
        -->
        <t t-call="website_form.field_text"/>
    </t>

    <!-- Integer Field -->
    <t t-name="website_form.field_integer">
        <t t-call="website_form.field">
            <input
                type="number"
                class="form-control o_website_form_input"
                t-att-name="field.name"
                step="1"
                t-att-required="field.required"
            />
        </t>
    </t>

    <!-- Float Field -->
    <t t-name="website_form.field_float">
        <t t-call="website_form.field">
            <input
                type="number"
                class="form-control o_website_form_input"
                t-att-name="field.name"
                step="any"
                t-att-required="field.required"
            />
        </t>
    </t>

    <!-- Date Field -->
    <t t-name="website_form.field_date">
        <t t-call="website_form.field">
            <t t-set="datepickerID" t-value="_.uniqueId('datepicker')"/>
            <div class="o_website_form_date input-group date" t-att-id="datepickerID" data-target-input="nearest">
                <input
                        type="text"
                        class="form-control datetimepicker-input o_website_form_input"
                        t-attf-data-target="##{datepickerID}"
                        t-att-name="field.name"
                        t-att-required="field.required"
                />
                <div class="input-group-append" t-attf-data-target="##{datepickerID}" data-toggle="datetimepicker">
                    <div class="input-group-text"><i class="fa fa-calendar"></i></div>
                </div>
            </div>
        </t>
    </t>

    <!-- Datetime Field -->
    <t t-name="website_form.field_datetime">
        <t t-call="website_form.field">
            <t t-set="datetimepickerID" t-value="_.uniqueId('datetimepicker')"/>
            <div class="o_website_form_datetime input-group date" t-att-id="datetimepickerID" data-target-input="nearest">
                <input
                        type="text"
                        class="form-control datetimepicker-input o_website_form_input"
                        t-attf-data-target="##{datetimepickerID}"
                        t-att-name="field.name"
                        t-att-required="field.required"
                />
                <div class="input-group-append" t-attf-data-target="##{datetimepickerID}" data-toggle="datetimepicker">
                    <div class="input-group-text"><i class="fa fa-calendar"></i></div>
                </div>
            </div>
        </t>
    </t>

    <!-- Boolean Field -->
    <t t-name="website_form.field_boolean">
        <t t-call="website_form.field">
            <div class="col-form-label">
                <input
                    type="checkbox"
                    value="Yes"
                    class="o_website_form_input"
                    t-att-name="field.name"
                    t-att-required="field.required"
                />
            </div>
        </t>
    </t>

    <!-- Selection Field -->
    <t t-name="website_form.field_selection">
        <t t-call="website_form.field">
            <div class="o_website_form_flex" data-display="horizontal">
                <t t-foreach="field.selection" t-as="option">
                    <div class="radio o_website_form_flex_item">
                        <label class="col-form-label">
                            <input
                                type="radio"
                                class="o_website_form_input"
                                t-att-name="field.name"
                                t-att-value="option[0]"
                                t-att-required="field.required"
                            />
                            <span>
                                <t t-esc="option[1]"/>
                            </span>
                        </label>
                    </div>
                </t>
            </div>
        </t>
    </t>

    <!-- Many2One Field -->
    <t t-name="website_form.field_many2one">
        <!-- Binary one2many -->
        <t t-if="field.relation == 'ir.attachment'">
            <t t-call="website_form.field_binary"/>
        </t>
        <!-- Generic one2many -->
        <t t-if="field.relation != 'ir.attachment'">
            <t t-call="website_form.field">
                <select class="form-control o_website_form_input" t-att-name="field.name" t-att-required="field.required">
                    <t t-foreach="field.records" t-as="record">
                        <option t-att-value="record.id" t-att-selected="record.selected">
                            <t t-esc="record.display_name"/>
                        </option>
                    </t>
                </select>
            </t>
        </t>
    </t>

    <!-- One2Many Field -->
    <t t-name="website_form.field_one2many">
        <!-- Binary one2many -->
        <t t-if="field.relation == 'ir.attachment'">
            <t t-call="website_form.field_binary">
                <t t-set="multiple" t-value="1"/>
            </t>
        </t>
        <!-- Generic one2many -->
        <t t-if="field.relation != 'ir.attachment'">
            <t t-call="website_form.field">
                <div class="o_website_form_flex" data-display="horizontal">
                    <t t-if="!field.records">
                        <input
                            class="o_website_form_input"
                            t-att-name="field.name"
                            t-att-value="record.id"
                            t-att-required="field.required"
                            placeholder="No matching record !"
                        />
                    </t>
                    <t t-foreach="field.records" t-as="record">
                        <t t-call="website_form.checkbox"/>
                    </t>
                </div>
            </t>
        </t>
    </t>

    <!-- Checkbox -->
    <t t-name="website_form.checkbox">
        <div class="checkbox o_website_form_flex_item">
            <label class="col-form-label">
                <input
                    type="checkbox"
                    class="o_website_form_input"
                    t-att-name="field.name"
                    t-att-value="record.id"
                    t-att-required="field.required"
                />
                <span>
                    <t t-esc="record.display_name"/>
                </span>
            </label>
        </div>
    </t>

    <!-- Many2Many Field -->
    <t t-name="website_form.field_many2many">
        <t t-call="website_form.field_one2many"/>
    </t>

    <!-- Binary Field -->
    <t t-name="website_form.field_binary">
        <t t-call="website_form.field">
            <input
                type="file"
                class="form-control o_website_form_input"
                t-att-name="field.name"
                t-att-required="field.required"
                t-att-multiple="multiple"
            />
        </t>
    </t>

    <!-- Monetary Field -->
    <t t-name="website_form.field_monetary">
        <t t-call="website_form.field_float" />
    </t>
</templates>

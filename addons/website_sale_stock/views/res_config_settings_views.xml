<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.website.sale.stock</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="website_sale.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@id='product_availability_setting']" position="attributes">
                <attribute name="title">Manage the inventory of your products and display their availability status on the website.</attribute>
            </xpath>

            <xpath expr="//div[@name='stock_inventory_availability']" position="inside">
                <div class="content-group">
                    <div class="row mt16"
                        id="website_warehouse_setting"
                        groups="stock.group_stock_multi_warehouses">
                        <label for="website_warehouse_id" string="Warehouse" class="col-lg-3 o_light_label" />
                        <field name="website_warehouse_id"/>
                    </div>
                    <div class="row mt16"
                        id="allow_to_order_setting"
                        title="Default availability mode set on newly created storable products. This can be changed at the product level.">
                        <label for="allow_to_order" string="Mode" class="col-lg-3 o_light_label" />
                        <field name="allow_to_order" widget="radio" class="o_light_label"/>
                    </div>
                    <div class="row" id="available_treshold_setting">
                        <label for="available_threshold" string="Threshold" class="col-lg-3 o_light_label" />
                        <field name="available_threshold" class="oe_inline" attrs="{'required': [('allow_to_order', '=', 'available')]}"/>
                    </div>
                    <div class="row"
                        id="show_availability_setting"
                        title="Default visibility for custom messages.">
                        <label for="show_availability" string="Show availability" class="col-lg-3 o_light_label" />
                        <field name="show_availability"/>
                    </div><br/>
                    <div class="row" id="available_message_setting">
                        <label for="available_message" string="In stock" class="col-lg-3 o_light_label" />
                        <field name="available_message" options="{'height': 135}"/>
                    </div>
                    <div class="row" id="out_of_stock_message_setting">
                        <label for="out_of_stock_message" string="Out of Stock" class="col-lg-3 o_light_label" />
                        <field name="out_of_stock_message" options="{'height': 135}"/>
                    </div>
                    <div class="row" id="threshold_message_setting">
                        <label for="threshold_message" string="Below Threshold" class="col-lg-3 o_light_label" />
                        <field name="threshold_message" options="{'height': 135}" attrs="{'required': [('allow_to_order', '=', 'available')]}"/>
                    </div>
                </div>
            </xpath>
        </field>
    </record>

</odoo>

